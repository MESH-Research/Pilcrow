# README

`/docs/design/review-process-original.svg` was created with [Inkscape](http://www.inkscape.org/). 

To update images, make modifications to this SVG file and export the appropriate image to the `/docs/public` directory. Be sure to check the export filename and filepath are correct before exporting. 
