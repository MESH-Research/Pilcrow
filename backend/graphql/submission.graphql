"""
A Submission
"""
type Submission {
    id: ID!
    title: String
    created_at: DateTime!
    updated_at: DateTime
    publication: Publication!
    users: [User!]! @belongsToMany
    pivot: SubmissionUser
}

type SubmissionUser {
    id: ID!
    role_id: ID!
}
