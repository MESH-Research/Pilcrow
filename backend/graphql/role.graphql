"""
A user role
"""
type Role @model(class: "Silber\\Bouncer\\Database\\Role") {
    id: ID!
    "Human-readable role title (e.g. \"Application Administrator\")."
    name: String! @rename(attribute: "title")
    created_at: DateTime
    updated_at: DateTime
}
