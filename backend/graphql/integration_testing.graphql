extend type Mutation {
    """
    Reset the database and run seeders.
    Only available in local and testing environments.
    """
    resetDatabase: String!
        @show(env: ["local", "testing"])
        @field(resolver: "App\\GraphQL\\Mutations\\ResetDatabase")
}
