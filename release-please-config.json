{
    "separate-pull-requests": true,
    "packages": {
        ".": {
            "release-type": "node",
            "exclude-paths": ["helm", ".lando"]
        },
        "helm": {
            "package-name": "pilcrow-helm",
            "release-type": "helm"
        }
    }
}
